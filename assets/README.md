## Assets Layout

- `audio/`: runtime audio files used by the game.
- `source_art/`: original imported art, menu concepts, and raw media used as design references.

Runtime-ready images and videos that the app actually serves should stay under `frontend/app/public/`.
